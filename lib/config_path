# This script need the coreutils installed on the macos

SCRIPT=$(greadlink -f "$0")

SCRIPTPATH=$(dirname "$SCRIPT")

echo "\$1: $1"
echo "\$0: $0"
echo "script: $SCRIPT"
echo "script path: $SCRIPTPATH"

echo "export PATH=\$PATH:$SCRIPTPATH" > tmp